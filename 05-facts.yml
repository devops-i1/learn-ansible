- name: Demo on Facts
  hosts: localhost
  tasks:
    - name: Printing hostname
      ansible.builtin.debug:
        msg: Hostname - {{ ansible_hostname }}   # ansible_hostname is a spl variable






# facts is nothing but data generated by ansible about remote node.

